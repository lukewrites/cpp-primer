#include <iostream>

int main(int argc, char const *argv[])
{
    // int ival = 1.01  <- that's not an int

    // int &rvall = 1.01;  <- that's not an int

    // int &rvall = 1;  <-- not valid.

    // int &rval3; <-- no initializer

    return 0;
}
